import { ValiditeNumberDirective } from './validite-number.directive';

describe('ValiditeNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new ValiditeNumberDirective();
    expect(directive).toBeTruthy();
  });
});
