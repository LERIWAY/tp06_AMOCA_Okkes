import { ValiditeStringDirective } from './validite-string.directive';

describe('ValiditeStringDirective', () => {
  it('should create an instance', () => {
    const directive = new ValiditeStringDirective();
    expect(directive).toBeTruthy();
  });
});
